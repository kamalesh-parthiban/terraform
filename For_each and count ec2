provider "aws" {
region = "ap-south-1"
}

resource "aws_instance" "multi" {

for_each = toset(["one", "two", "three"])                   #count = 100

ami = "ami.."
key_name = ""
instance_type = "t3.micro"

tags {
Name = each.value
}
}

#for count

  tags = {
    Name = "ec2-${count.index}"
  }
}
